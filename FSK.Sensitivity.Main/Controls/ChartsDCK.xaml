<UserControl x:Class="FSK.Sensitivity.Main.Controls.ChartsDCK"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSK.Sensitivity.Main.Controls"
             mc:Ignorable="d"  Background="White"
             Loaded="UserControl_Loaded" SizeChanged="UserControl_SizeChanged"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Margin="0,5,0,0"  HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="14"   >
<Run Text="标准检查结果【VA="/>
<Run Text="{Binding TitleResult,RelativeSource = {RelativeSource Mode=FindAncestor, AncestorType=local:ChartsDCK}}"/>
<Run Text="】"/>
        </TextBlock>
        <Border Grid.Row="1"   >
            <Canvas x:Name="myCanvas"   />
        </Border>
    </Grid>
</UserControl>
