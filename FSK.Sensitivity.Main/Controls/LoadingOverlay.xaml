<UserControl x:Class="FSK.Sensitivity.Main.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSK.Sensitivity.Main.Controls"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <!-- 半透明背景 -->
        <Border Background="Black" Opacity="0.7"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
            <!-- 这里放置你的加载动画 -->
            <hc:LoadingCircle Width="200" Height="200" DotDiameter="30"/>
            <TextBlock Text="{Binding LoadingText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"
                       FontSize="{StaticResource FontSize.Title}"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       MaxWidth="300"/>
        </StackPanel>
    </Grid>
</UserControl>
