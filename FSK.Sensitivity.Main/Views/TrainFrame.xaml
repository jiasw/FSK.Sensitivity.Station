<UserControl x:Class="FSK.Sensitivity.Main.Views.TrainFrame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSK.Sensitivity.Main.Views"
             xmlns:hc="https://handyorg.github.io/handycontrol"
xmlns:prism="http://prismlibrary.com/"
prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="290"/>
            </Grid.ColumnDefinitions>
            <Button  Command="{Binding BackCommand}" VerticalAlignment="Center" hc:IconElement.Geometry="{StaticResource BackGeometry}" hc:IconElement.Width="55"  Style="{DynamicResource NavButton}"  Width="80" Height="70"></Button>
            <Button Command="{Binding ListResultCommand}" Visibility="{Binding ShowLogButton}" Height="70" Grid.Column="3"  Content="检查记录" hc:IconElement.Geometry="{StaticResource PageModeGeometry}" hc:IconElement.Width="35" hc:IconElement.Height="35"  Style="{DynamicResource NavButton}"></Button>

        </Grid>
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="TrainRegion"  ></ContentControl>
    </Grid>
</UserControl>
