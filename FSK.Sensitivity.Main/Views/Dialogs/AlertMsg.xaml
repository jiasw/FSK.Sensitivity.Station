<UserControl x:Class="FSK.Sensitivity.Main.Views.Dialogs.AlertMsg"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSK.Sensitivity.Main.Views.Dialogs"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="WindowStyle" Value="None"></Setter>
            <Setter Property="AllowsTransparency" Value="True"></Setter>
            <Setter Property="Width" Value="550"></Setter>
            <Setter Property="SizeToContent" Value="Height"/>
            <Setter Property="Topmost" Value="True"></Setter>
            <Setter Property="MinHeight" Value="300"></Setter>
            <Setter Property="Background" Value="Transparent"></Setter>
        </Style>
    </prism:Dialog.WindowStyle>
    <Border CornerRadius="10" Background="{StaticResource SecondBackGroundBrush}"  BorderBrush="{StaticResource DarkDefaultBrush}" BorderThickness="1">
        
        <Grid  >

            
            <Grid.RowDefinitions>
                <RowDefinition Height="80" />
                <RowDefinition Height="*" />
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" />
            <TextBlock Grid.Row="1" TextWrapping="WrapWithOverflow" Text="{Binding Msg}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" />
            <Button   Grid.Row="2"   Content="确定" FontSize="26" Width="150" Background="{StaticResource PrimaryBrush}" Margin="0,10" Style="{StaticResource NavButton}" HorizontalAlignment="Center" VerticalAlignment="Center"  Command="{Binding CloseCommand}" />




        </Grid>
    </Border>

</UserControl>
