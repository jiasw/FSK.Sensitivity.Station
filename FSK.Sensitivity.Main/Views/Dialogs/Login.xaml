<UserControl x:Class="FSK.Sensitivity.Main.Views.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSK.Sensitivity.Main.Views"
              xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" Width="700" Height="450"
             d:DesignHeight="450" d:DesignWidth="700">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
            <Setter Property="WindowStyle" Value="None"></Setter>
            <Setter Property="AllowsTransparency" Value="True"></Setter>
            <Setter Property="Background" Value="Transparent"></Setter>
        </Style>
    </prism:Dialog.WindowStyle>
    <Border CornerRadius="10" Background="{DynamicResource DarkDefaultBrush}" >
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>
            <Border BorderThickness="3" BorderBrush="White" CornerRadius="10" Margin="10">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Patients}">
                        <!-- 性能优化：虚拟化支持 -->
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <!-- 使用样式触发器避免转换器 -->
                                <TextBlock Text="{Binding Name}" Padding="10">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="FontSize" Value="24"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="Background" Value="LightSkyBlue"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                
                <!-- 命令绑定（无需后台代码）-->
                                    <TextBlock.InputBindings>
                                        <MouseBinding Gesture="LeftClick"  CommandParameter="{Binding}"
                                  Command="{Binding Path=DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    </TextBlock.InputBindings>
                                </TextBlock>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />

                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width=".5*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="用户名" FontSize="24" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10,10" />
                    <TextBox Grid.Column="1" Text="{Binding Username, Mode=TwoWay}" Padding="5,5" FontSize="24" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                    <UniformGrid Columns="2" Grid.Row="2" Grid.Column="1" Height="50">
                        <Button  Content="登录" HorizontalAlignment="Center" Width="80" Height="40" FontSize="22" Style="{DynamicResource NormalButton}" VerticalAlignment="Center"  Command="{Binding LoginCommand}" />
                        <Button  Content="取消" HorizontalAlignment="Center" Width="80"  Height="40" FontSize="22" Style="{DynamicResource NormalButton}" VerticalAlignment="Center"  Command="{Binding CancelCommand}" />
                    </UniformGrid>


                </Grid>

            </Grid>

        </Grid>
    </Border>
    
</UserControl>
