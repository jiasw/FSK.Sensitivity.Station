<UserControl x:Class="FSK.Sensitivity.Main.Views.UserInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSK.Sensitivity.Main.Views"
             xmlns:ctl="clr-namespace:FSK.Sensitivity.Main.Controls"
             mc:Ignorable="d" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:hc="https://handyorg.github.io/handycontrol"
xmlns:prism="http://prismlibrary.com/"
prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <b:Interaction.Triggers>
        <b:EventTrigger  EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Content="新增" Style="{StaticResource NavButton}" Width="120" HorizontalAlignment="Right" Margin="50,10" Command="{Binding AddMangerCommand}"></Button>
        <ListView Grid.Row="1" Margin="50,0" Style="{StaticResource MyCommonListViewStyle}" ItemsSource="{Binding Mangers}" >
            

            <ListView.View>
                <GridView>

                    <GridViewColumn Width="180" >
                        <GridViewColumn.Header>
                            <TextBlock Text="姓名"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock  
    Text="{Binding Name}"  Style="{StaticResource ListViewTextStyle}"  
/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>


                    <GridViewColumn Width="80" >
                        <GridViewColumn.Header >
                            <TextBlock Text="性别"   />
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock   Text="{Binding gender_desc}" Style="{StaticResource ListViewTextStyle}"    />
                                
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="80" Header="年龄"  >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock   Text="{Binding Age}" Style="{StaticResource ListViewTextStyle}"     />

                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="180" Header="联系电话"  >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock    Text="{Binding Phone}"  Style="{StaticResource ListViewTextStyle}"   />

                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="120" Header="人员类别" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock   Text="{Binding type_desc}"  Style="{StaticResource ListViewTextStyle}"   />

                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="操作" Width="180" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel  Orientation="Horizontal" >
                                    <Button Content="编辑" Padding="10,0"  Margin="10,0"  FontSize="20" Height="35" Style="{StaticResource ButtonInfo}" Command="{Binding DataContext.EditMangerCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"  CommandParameter="{Binding}"   />
                                    <Button Content="删除" Padding="10,0"  Margin="10,0"  FontSize="20" Height="35" Style="{StaticResource ButtonDanger}" Command="{Binding DataContext.DeleteMangerCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        
        <ctl:Pagination Grid.Row="2"  Margin="0,10" TotalPages="{Binding  TotalPage}" CurrentPage="{Binding PageIndex,Mode=TwoWay}" PreviousCommand="{Binding PrevCmd}" NextCommand="{Binding NextCmd}" >

        </ctl:Pagination>
    </Grid>
</UserControl>
