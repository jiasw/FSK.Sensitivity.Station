<UserControl x:Class="FSK.Sensitivity.Main.Views.HardWareTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSK.Sensitivity.Main.Views"
             mc:Ignorable="d" 
                          xmlns:hc="https://handyorg.github.io/handycontrol"
xmlns:prism="http://prismlibrary.com/"
prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel>
            <StackPanel Orientation="Horizontal"  Margin="10,10,0,0">
                <Button Content="开-灯板" Command="{Binding LightCommand}" CommandParameter="1"></Button>
                <Button Content="关-灯板"  Command="{Binding LightCommand}" CommandParameter="0"></Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,10,0,0">
                <Button Content="初始化所有电机" Command="{Binding InitMotorCommand}" CommandParameter="0"></Button>
            </StackPanel>



            <StackPanel Orientation="Horizontal"  Margin="10,10,0,0">
                <Button Content="设置左侧转盘"></Button>
                <TextBlock Text="转盘角度:" VerticalAlignment="Center" ></TextBlock>
                <TextBox Width="100" Text="{Binding LeftClampPosition}"></TextBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal"  Margin="10,10,0,0">
                <Button Content="设置右侧转盘"></Button>
                <TextBlock Text="转盘角度:"  VerticalAlignment="Center"></TextBlock>
                <TextBox Width="100" Text="{Binding RightClampPosition}"></TextBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal"  Margin="10,10,0,0">
                <Button Content="设置丝杆间距"></Button>
                <TextBlock Text="丝杆间距:"></TextBlock>
                <TextBox Width="100" Text="{Binding SlideBlockPosition}" ></TextBox>
            </StackPanel>


        </StackPanel>
        <StackPanel Grid.Column="1">
            <Button Content="{Binding BtnJoystickContentProperty}" Command="{Binding StartOrStopJoystickCommand}"></Button>
            <TextBox TextWrapping="WrapWithOverflow" Margin="10,10,10,10" Height="200" Width="200" Text="{Binding JoystickStatus}"></TextBox>
            
        </StackPanel>
    </Grid>
</UserControl>
