<UserControl x:Class="FSK.Sensitivity.Main.Views.SerialPortConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:FSK.Sensitivity.Main.Views"
             xmlns:hc="https://handyorg.github.io/handycontrol"
xmlns:prism="http://prismlibrary.com/"
prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" Height="715" Width="960">
    <b:Interaction.Triggers>
        <b:EventTrigger  EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="serialPortConfigTitle">
            <Setter Property="FontSize" Value="48" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="30,25" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Width" Value="300" />
            <Setter Property="TextAlignment" Value="Right"></Setter>
        </Style>
        <Style TargetType="ComboBox" x:Key="serialPortConfigComboBox" BasedOn="{StaticResource ComboBoxBaseStyle}">
            <Setter Property="Width" Value="350" />
            <Setter Property="Height" Value="65" />
            <Setter Property="FontSize" Value="30"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" >
            <StackPanel Orientation="Horizontal">
                <TextBlock  Text="硬件串口:" Style="{StaticResource serialPortConfigTitle}" />
                <ComboBox   Style="{StaticResource serialPortConfigComboBox}" ItemsSource="{Binding SerialPortNames}" SelectedValue="{Binding MotorSerialPortName}" />
            </StackPanel>
            
            <Button  Content="保存" FontSize="26" Width="250" Margin="0,30" Style="{StaticResource NavButton}" Command="{Binding SaveCommand}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
