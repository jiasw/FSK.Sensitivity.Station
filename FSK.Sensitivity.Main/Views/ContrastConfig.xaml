<UserControl x:Class="FSK.Sensitivity.Main.Views.ContrastConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSK.Sensitivity.Main.Views"
             xmlns:lclcv="clr-namespace:FSK.Sensitivity.Main.Converters"
                  xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
xmlns:enum = "clr-namespace:FSK.Sensitivity.Core.Enums;assembly=FSK.Sensitivity.Core"
         mc:Ignorable="d" 
         xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="600" d:DesignWidth="1200">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="ContrastParamStyle" TargetType="Border" BasedOn="{StaticResource ConfigParamBorderStyle}">
                <Setter Property="Margin" Value="20,0"/>
                <Setter Property="Height" Value="250"></Setter>
                <Setter Property="Width" Value="300"></Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Grid  HorizontalAlignment="Center" DataContext="{Binding ContrastConfigParam}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Style="{DynamicResource ContrastParamStyle}">
                <i:Interaction.Triggers>
                    <!-- 处理左键点击 -->
                    <i:EventTrigger EventName="TouchDown">
                        <i:InvokeCommandAction 
                Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl},
 Path=DataContext.ShowItemsDialogCommand}"
                CommandParameter="{x:Static enum:ItemsType.DarkTime}"/>
                    </i:EventTrigger>
                    <!-- 处理左键点击 -->
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction 
Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl},
 Path=DataContext.ShowItemsDialogCommand}"
CommandParameter="{x:Static enum:ItemsType.DarkTime}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="检查时间" Style="{DynamicResource ConfigParamTitleStyle}"></TextBlock>
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding CheckDuration,Converter={StaticResource EnumToDescriptionConverter}}"  VerticalAlignment="Center" FontSize="60" Foreground="White"></TextBlock>
                            <TextBlock Text="S"  FontWeight="Bold" Padding="0,0,0,5" VerticalAlignment="Bottom" FontSize="40" Margin="5,0,0,0" Foreground="White"></TextBlock>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Border Style="{DynamicResource ContrastParamStyle}" Grid.Column="2">
                <i:Interaction.Triggers>
                    <!-- 处理左键点击 -->
                    <i:EventTrigger EventName="TouchDown">
                        <i:InvokeCommandAction 
                Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl},
 Path=DataContext.ShowItemsDialogCommand}"
                CommandParameter="{x:Static enum:ItemsType.PD}"/>
                    </i:EventTrigger>
                    <!-- 处理左键点击 -->
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction 
Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl},
 Path=DataContext.ShowItemsDialogCommand}"
CommandParameter="{x:Static enum:ItemsType.PD}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="瞳距" Style="{DynamicResource ConfigParamTitleStyle}"></TextBlock>
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding PD}"  VerticalAlignment="Center" FontSize="60" Foreground="White"></TextBlock>
                            <TextBlock Text="mm"  FontWeight="Bold" Padding="0,0,0,5" VerticalAlignment="Bottom" FontSize="40" Margin="5,0,0,0" Foreground="White"></TextBlock>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
        <UniformGrid Grid.Row="1" Columns="2" Margin="10"  HorizontalAlignment="Center">
            <Button Content="确认" Style="{DynamicResource btnOprateStyle}" Command="{Binding SaveCommand}" Background="{DynamicResource MainBrush}"   Margin="50,5"   ></Button>
            <Button Content="取消" Style="{DynamicResource btnOprateStyle}" Command="{Binding BackCommand}" BorderThickness="2" Background="{DynamicResource SecondBackGroundBrush}"   Margin="50,5"  ></Button>
        </UniformGrid>
    </Grid>
</UserControl>
